export default function AccountPage() {
    return (
        <main className="page-gutter mx-auto flex min-h-screen w-full max-w-3xl flex-col justify-center gap-5 py-12">
            <p className="text-sm font-semibold uppercase tracking-[0.2em] text-black/50">SHOP.COM</p>
            <h1 className="text-4xl font-bold text-black">Your account</h1>
            <p className="max-w-xl text-lg leading-relaxed text-black/60">Account sign-in and order history will be available when authentication is connected.</p>
        </main>
    );
}
